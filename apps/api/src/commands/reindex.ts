import { NestFactory } from '@nestjs/core'; import { DocumentReindexService } from '../modules/documents/document-reindex.service';
export function parseReindexArgs(args:string[]){const index=args.indexOf('--document-id');const documentId=index >= 0 ? args[index+1] : undefined;const missing=args.includes('--missing');const dryRun=args.includes('--dry-run');if(Boolean(documentId)===missing||index>=0&&!documentId)throw new Error('Use exactly one of --document-id <id> or --missing');return {documentId,missing,dryRun};}
export async function runReindex(args=process.argv.slice(2)){const { AppModule }=await import('../app.module.js');const app=await NestFactory.createApplicationContext(AppModule);try{const result=await app.get(DocumentReindexService).run(parseReindexArgs(args));console.log(JSON.stringify(result));return result;}finally{await app.close();}}
